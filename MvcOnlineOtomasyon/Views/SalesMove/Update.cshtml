@model MvcOnlineOtomasyon.Models.Concrete.SalesMove
@{
    ViewBag.Title = "Update";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<div class="container">
    <h2>Satış Güncelleme Sayfası</h2>
    @using (Html.BeginForm("Update", "SalesMove", FormMethod.Post))
    {

        <div class="form-group">
            @Html.HiddenFor(x => x.SalesMoveId)
        </div>
        <div class="form-group">
            <label><b>Ürün Adı</b></label>
            @Html.DropDownListFor(x => x.ProductId, (List<SelectListItem>)ViewBag.v1, new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label><b>Ürün Fiyat</b></label>
            @Html.TextBoxFor(x => x.Price, new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label><b>Adet</b></label>
            @Html.TextBoxFor(x => x.Quantity, new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label><b>Toplam Fiyat</b></label>
            @Html.TextBoxFor(x => x.SumPrice, new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label><b>Tarih</b></label>
            @Html.TextBoxFor(x => x.Date, new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label><b>Personel</b></label>
            @Html.DropDownListFor(x => x.PersonelId, (List<SelectListItem>)ViewBag.v2, new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label><b>Müşteri</b></label>
            @Html.DropDownListFor(x => x.CustomerId, (List<SelectListItem>)ViewBag.v3, new { @class = "form-control" })
        </div>

        <div class="form-group">
            <button class="btn btn-outline-warning float-right">Güncelle</button>
            <a href="/SalesMove/Index" class="btn btn-outline-danger">Vazgeç</a>
        </div>
    }
</div>

